# Problem 42 — Replace Words

## 📝 Problem Statement

Write a program to replace words in a string.

In C++, the `replace()` function of the `std::string` class can replace part of a string with another string.
