# Problem 43 — Replace Words (Custom)

## 📝 Problem Statement

Write a program to replace words in a string using a custom function.

The function should support matching the case or ignoring the case when deciding whether a word should be replaced.
